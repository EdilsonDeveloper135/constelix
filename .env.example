# Preferred LLM settings. They are read only by the local agent process.
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o
LLM_API_KEY=

# Legacy aliases remain supported when the preferred variables are unset.
OPENAI_API_KEY=
CONSTELIX_OPENAI_MODEL=
